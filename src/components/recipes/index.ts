import { cva } from "../../../styled-system/css";

export const select = cva({
  base: {
    border: "none",
    borderRadius: "4",
    p: "2",
  },
});

export const input = cva({
  base: {
    border: "none",
    borderRadius: "4",
  },
});
